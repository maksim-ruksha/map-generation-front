import React from "react";
import {Card} from "@mui/material";

export default function MapItemComponent({
                                         id,
                                         name,
                                         authorName,
                                         previewLink,
                                         description
                                     }) {
    return <Card>

    </Card>
}